{
  "name": "vitest-test",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest --watch --coverage",
    "webstorm-integration": "vitest --watch --coverage --reporter=dot --reporter=json --outputFile=.vitest-result.json"
  },
  "devDependencies": {
    "@vitest/coverage-c8": "^0.22.1",
    "c8": "^7.12.0",
    "typescript": "^4.6.4",
    "vite": "^3.0.7",
    "vitest": "^0.22.1"
  }
}
